#!/usr/bin/env bash
set -euo pipefail

SWAYOSD_LAUNCHER="${SWAYOSD_LAUNCHER:-$HOME/bin/launch-swayosd}"
[[ "$SWAYOSD_LAUNCHER" == /* && -x "$SWAYOSD_LAUNCHER" ]] || exit 1
exec "$SWAYOSD_LAUNCHER" --restart "$@"
