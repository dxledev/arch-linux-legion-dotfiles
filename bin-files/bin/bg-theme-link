#!/usr/bin/env bash

theme="$1"

base_dir="$HOME/files/pictures/wallpapers"
theme_dir="$base_dir/$theme"

rm -rf wallpaper.png wallpapers

ln -sfn "$theme_dir" wallpapers

ln -sf \
"$(find "$theme_dir" -maxdepth 1 -type f -name '1-*' | sort | head -n 1)" \
wallpaper.png
